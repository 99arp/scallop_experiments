[project]
name = "scallop"
version = "0.1.0"
description = "Salvaged STL drone rule monitor and Graphviz visualizer"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jax>=0.4.13",
    "maturin>=1.13.3",
    "pyaml>=26.2.1",
]

[project.scripts]
stl-monitor = "stl.main:main"
